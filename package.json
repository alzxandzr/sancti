{
  "name": "sancti",
  "version": "0.1.0",
  "private": true,
  "description": "Catholic saint-matching and devotional planning app scaffold",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
